"""Gemini Developer API defaults and UI presets (no heavy SDK imports)."""

DEFAULT_GEMINI_MODEL = "gemini-2.0-flash"

# Shown in add-on settings; any model id supported by the API can be entered.
GEMINI_MODEL_CHOICES = [
    "gemini-2.0-flash",
    "gemini-2.5-flash",
    "gemini-2.5-pro",
    "gemini-2.5-flash-lite",
]
